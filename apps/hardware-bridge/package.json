{
  "name": "hardware-bridge",
  "version": "0.0.1",
  "private": true,
  "description": "PURA local hardware agent (loopback mock adapters)",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "start": "node dist/main.js",
    "dev": "npx tsx src/main.ts"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
